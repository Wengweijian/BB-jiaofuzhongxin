# 动作积分自动统计工具（复刻版）

> 复刻自原「动作积分自动统计脚本」（PyInstaller 打包的 Windows GUI 程序，带联网授权校验）。
> 本复刻版：**去掉授权校验、逻辑完全一致、无需联网**，并额外提供一个**零安装的浏览器版**。

## 它做什么

输入两份 Excel：

1. **聊天记录文件**（微信/企微导出的群聊记录，需含 `发送人`、`内容` 两列）
2. **积分公示表（模版）**（门店=行、动作=列的空表）

程序自动：

- 按「门店关键字」把每条聊天记录归到对应门店（发送人里含该关键字即命中）
- 按「动作关键字配置」统计**每条内容命中几个动作关键字**，累加到该门店该动作
- 每个动作有**上限**（limit），累加超过上限则封顶
- 把结果写回模版的「门店行 × 动作列」单元格；统计为 0 的按设置处理（空白/0/跳过）

## 模版结构约定（默认）

| 项 | 默认 | 说明 |
|:--|:--|:--|
| 门店关键字列 | **H 列** | 从**第 5 行**起，逐行向下读取门店关键字 |
| 动作标题行 | **第 4 行** | 该行放动作名称 |
| 动作标题起始列 | **J 列** | 从 J 列向右读标题，遇到空单元格停止 |
| 0 值处理 | blank | blank=留空 / zero=写0 / skip=不动 |

> 行列都可自定义（页面/命令参数里改）。

## 两种用法

### 方式一：浏览器版（推荐，零安装）—— `动作积分自动统计工具.html`

- 双击用浏览器打开（Chrome / Edge 均可），**纯本地运行，数据不上传**
- 依次选「聊天记录」「积分公示表」，选 Sheet，确认列配置，点「开始分析处理」
- 会自动下载一份 `xxx_已统计.xlsx`，并展示统计结果表
- 动作关键字可直接在页面上增删改
- ⚠️ 说明：浏览器版尽量保留模版原有格式；若你的模版格式非常复杂，建议用 Python 版（openpyxl 原样保留格式）

### 方式二：Python 版（格式保留最稳）—— `jifen_tongji.py`

```bash
pip install openpyxl

# 直接写回模版
python jifen_tongji.py --chat 聊天记录.xlsx --template 模版.xlsx

# 另存为新文件（不动原模版）
python jifen_tongji.py --chat 聊天记录.xlsx --template 模版.xlsx --out 结果.xlsx

# 指定列/Sheet
python jifen_tongji.py --chat a.xlsx --template b.xlsx \
  --chat-sheet 聊天记录 --template-sheet 公示 \
  --keyword-col H --title-row 4 --title-col J

# 图形界面（需本机有 tkinter，通常是 Python 自带的）
python jifen_tongji.py --gui
```

## 关键字配置（`keyword_config.json`）

首次运行会自动生成，格式：

```json
{
  "_settings": { "zero_handling": "blank" },
  "每日晨读":   { "keywords": ["晨读","早读","晨会","一读"], "limit": 2, "description": "每日晨读活动统计" },
  "3+X产品演练": { "keywords": ["产品演练","产品培训","演练"], "limit": 5 },
  "上门测量":   { "keywords": ["测量"], "limit": 10 },
  "门店落地":   { "keywords": ["落地"], "limit": 3 },
  "破零攻坚":   { "keywords": ["添加","加微信"], "limit": 10 },
  "订单晒单":   { "keywords": ["订单晒单"], "limit": 5 },
  "日清":       { "keywords": ["日清","工作日志","总结"], "limit": 1 }
}
```

- `keywords`：命中词（内容里出现**任意一个**即算命中该动作；一条内容命中 N 个不同关键字就计 N 分）
- `limit`：该动作每人每天最多计入的分数上限
- 动作名称需与模版第 4 行的标题**完全一致**才会生效

## 打包成 Windows 单文件 exe（可选）

在 Windows 上执行：

```bash
pip install pyinstaller openpyxl
pyinstaller -F -w jifen_tongji.py -n 积分统计工具
# 生成的 dist/积分统计工具.exe 双击即用
```

（`-w` 表示 GUI 无控制台窗口；如需命令行版本去掉 `-w`。）

## 与原版的差异

| 项 | 原版 | 复刻版 |
|:--|:--|:--|
| 授权校验 | 联网校验 key（服务器 skcwxserver.sunxihe.cn） | **无**，离线可用 |
| 逻辑 | 门店归集 + 动作关键字计数 + 上限封顶 | **完全一致** |
| 使用方式 | Windows GUI exe | 浏览器版 + Python 版 + GUI |
| 数据安全 | 需联网校验 | 全程本地 |
